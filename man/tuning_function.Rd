% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning_cross_validation.R
\name{tuning_function}
\alias{tuning_function}
\title{Tuning Function for Elastic Net Hyperparameters}
\usage{
tuning_function(alpha, X_train, yhat_train, fold_id, lambda_choice)
}
\arguments{
\item{alpha}{Numeric value representing the alpha parameter in elastic net, controlling the mix
of L1 and L2 regularization. Expected range: \link{0, 1}.}

\item{X_train}{Training predictors, a matrix or data frame of SNP data.
Dimensions: Observations (rows) x SNPs (columns).}

\item{yhat_train}{Numeric vector representing the response variable in the training set.
Length should match the number of rows in X_train.}

\item{fold_id}{Numeric vector specifying fold assignments for cross-validation.}

\item{lambda_choice}{Character string indicating the method for choosing lambda
('min' for minimum MSE, '1se' for one-standard-error rule).}
}
\value{
A numeric vector containing the mean squared error, optimal lambda, and alpha.
}
\description{
Performs hyperparameter tuning for elastic net models, specifically tailored for
methylome-wide association studies. It iterates over a range of alpha values to find the optimal
lambda using cross-validation.
}
\examples{
\dontrun{
optimal_parameters <- tuning_function(alpha, X_train, yhat_train, fold_id, lambda_choice)
}
}
